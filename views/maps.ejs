
<%- include("header.ejs", {
    "mode": "maps", 
    meta,
    host,
    userSession
    }) -%>

<%- include("timeZone.ejs", {timeZone})%>

<% 

const jsBase = (locals.bSeasonPage) ? `../../js/` : `../js/`;



%>
<script src="<%= jsBase %>ui/ui.js"></script>
<script src="<%= jsBase %>ui/table.js"></script>
<script src="<%= jsBase %>ui/maps.js"></script>

<div class="header-wrapper">
    Maps
</div>

<div id="root">
</div>

<script>
    const searchForm = new MapsSearchForm(
        "#root", 
        "<%= nameSearch %>", 
        "<%= sortBy %>",
        "<%= order %>",
        "<%= displayMode %>", 
        "<%= page %>",
        "<%= perPage %>",
        <%- JSON.stringify(maps) %>,
        <%= parseInt(totalMatches) %>,
        "<%= seasonId %>"
    );   
</script>


<%- include("footer.ejs") -%>